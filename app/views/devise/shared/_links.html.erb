<div class="mt-4 text-center">
  <% if controller_name != 'sessions' %>
    <%= link_to "Iniciar Sessão", new_session_path(resource_name), class: "btn btn-outline-primary mb-2 w-100" %>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to "Cadastre-se", new_registration_path(resource_name), class: "btn btn-outline-secondary w-100" %>
  <% end %>
</div>
